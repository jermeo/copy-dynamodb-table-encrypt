{
  "name": "copy-dynamodb-table-encrypt",
  "version": "1.0.3",
  "description": "Copy Dynamodb table to another in the same or different zone and encrypt if needed, It is 100% safe , and speed depends on your destination table user defined write provisioned throughput",
  "keywords": [
    "dynamodb",
    "aws-dynamodb",
    "AWS",
    "copy-tables",
    "encrypt"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jermeo/copy-dynamodb-table-encrypt"
  },
  "author": "Jermeo",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.56.0",
    "readline": "^1.3.0"
  }
}
